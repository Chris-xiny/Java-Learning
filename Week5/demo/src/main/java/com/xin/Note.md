Note 1:   线程安全问题_同步代码块
    原因:CPU在多个线程做高速切换
    解决方法1:使用同步代码块
        1.格式：
            synchronized(任意对象){
                线程可能出现不安全的代码
            }
        2.任意对象:锁对象(eg:两个人抢的一个厕所)
        3.执行:
            一个线程拿到锁后会进入同步代码块中执行，在此期间其他线程拿不到锁进不去同步代码块，
            只能等待有锁的线程执行完后释放锁才能抢锁
